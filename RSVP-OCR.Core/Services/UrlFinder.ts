class UrlFinder
{
    // Combs through a string of JSON for URLs or partial URLS
    // Adds all of them to respectively names list collections
    // Tries to find the URL from the navigation bar in paticular
    // Pings all of the urls to see which ones work or not
    // Adds working ones and non working ones to respictive lists
    // Attempts to reconsile the closest relivant URL if they're only partial or not working initially
}
